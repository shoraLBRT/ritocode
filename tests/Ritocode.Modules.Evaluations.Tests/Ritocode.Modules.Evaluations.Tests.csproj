<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="coverlet.collector" />
    <PackageReference Include="Microsoft.NET.Test.Sdk" />
    <PackageReference Include="xunit.v3" />
    <PackageReference Include="xunit.runner.visualstudio" />
  </ItemGroup>

  <!-- No TestSupport: nothing here touches a database or an object store. The sandbox runner's own tests
       start containers through the runner under test, so they need a Docker daemon and nothing else. -->
  <ItemGroup>
    <ProjectReference Include="../../src/Modules/Ritocode.Modules.Evaluations/Ritocode.Modules.Evaluations.csproj" />
  </ItemGroup>

  <ItemGroup>
    <Using Include="Xunit" />
  </ItemGroup>

</Project>
