# Copy to .env and adjust only if a port collides with something already running.
# These values are for local development only and are safe to commit as defaults.

POSTGRES_USER=ritocode
POSTGRES_PASSWORD=ritocode
POSTGRES_DB=ritocode
POSTGRES_PORT=55432

MINIO_ROOT_USER=ritocode
MINIO_ROOT_PASSWORD=ritocode-dev-secret
MINIO_PORT=59000
MINIO_CONSOLE_PORT=59001
